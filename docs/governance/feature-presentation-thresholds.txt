# format: feature|maxLines|reason
# 这些是迁移期仍留在 composeApp 的大体量 UI Feature 基线。
# 没有独立 feature:<name>:presentation 模块的 Feature 超过 800 行时必须登记；
# 后续继续增长会触发 checkLongTermGovernance，除非先拆分 Presentation 模块或更新治理说明。
# quickcreate 已有 feature:quickcreate:presentation，因此不需要在此登记。
create|2697|Legacy create UI retained only to keep migration compatibility while new creation flow moves to QuickCreate.
discovery|1208|Discovery UI shell remains in composeApp; state holder, sort labels, catalog error text and tests are now owned by feature:discovery:presentation.
history|1002|History UI/Voyager adapter remains in composeApp; state machine, polling, detail loading, cancellation and tests are now owned by feature:task:presentation.
login|834|Login UI shell and platform captcha dialog remain in composeApp; state machine, error text and tests are now owned by feature:auth:presentation.
profile|879|Profile UI shell remains in composeApp; state holder, credential operations and tests are now owned by feature:auth:presentation. P8 copy resourceization temporarily increases imports while keeping hardcoded ProfileScreen UI copy at zero.
