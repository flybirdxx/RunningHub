# format: feature|maxLines|ownerPresentation|reason
# 这些是迁移期仍留在 composeApp 的大体量 UI Feature 基线。
# 没有同名 feature:<name>:presentation 模块的 Feature 超过 800 行时必须登记；
# ownerPresentation 必须指向实际持有状态机的 feature:<owner>:presentation 模块。
# 后续超过登记基线的小幅缓冲后才会触发行数失败；状态机回流信号不进入缓冲，仍会直接触发 checkLongTermGovernance。
# 该缓冲只用于吸收小型 UI 适配、资源映射和必要注释，不允许继续承载可变状态流、协程编排、轮询或业务编排。
# quickcreate、discovery 已有 feature:<name>:presentation，因此不需要在此登记。
# create 当前没有遗留 Kotlin UI 文件，因此不需要保留历史行数基线。
history|1712|feature:task:presentation|History UI/Voyager 适配层仍留在 composeApp；TaskHistoryStateHolder 持有状态机、轮询、详情加载、取消和测试。本基线反映 2026-07-02 棘轮收紧后的当前体量，不允许状态机职责回流到 composeApp。
login|1022|feature:auth:presentation|Login UI shell and platform captcha dialog remain in composeApp; LoginStateHolder owns login state, stable error semantics and tests. Current baseline reflects Compose Resources and SMS captcha Web copy mapping only; no state-machine responsibility is allowed to return to composeApp.
profile|882|feature:auth:presentation|Profile UI shell remains in composeApp; ProfileStateHolder owns profile refresh, credential operations, logout state and tests. Current baseline reflects the 2026-07-02 ratchet after RM-13 wallet, membership, transaction empty-state cards and Compose Resources mapping while ProfileScreen and SettingsDialog hardcoded UI copy remain at zero.
plaza|1375|feature:community:presentation|Plaza UI 壳和 Voyager 适配层仍留在 composeApp；PlazaStateHolder 持有分页、筛选、降级语义和测试。本基线反映 2026-07-02 棘轮收紧后的当前体量（RM-12 使用同款卡片、参数复用确认和 Compose 资源适配），不允许状态机职责回流到 composeApp。
