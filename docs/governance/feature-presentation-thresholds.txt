# format: feature|maxLines|reason
# 这些是迁移期仍留在 composeApp 的大体量 UI Feature 基线。
# 没有独立 feature:<name>:presentation 模块的 Feature 超过 800 行时必须登记；
# 后续继续增长会触发 checkLongTermGovernance，除非先拆分 Presentation 模块或更新治理说明。
# quickcreate、discovery 已有 feature:<name>:presentation，因此不需要在此登记。
# create 当前没有遗留 Kotlin UI 文件，因此不需要保留历史行数基线。
history|1116|History UI/Voyager adapter remains in composeApp; state machine, polling, detail loading, cancellation and tests are now owned by feature:task:presentation. 2026-06-22 increase is limited to TaskHistoryScreen Compose Resources migration; no new state-machine responsibility was added.
login|856|Login UI shell and platform captcha dialog remain in composeApp; state machine, error text and tests are now owned by feature:auth:presentation. 2026-06-22 increase is limited to LoginScreen Compose Resources migration; no new state-machine responsibility was added.
plaza|805|Plaza UI shell remains in composeApp; state holder, filtering, paging and fallback logic are now owned by feature:community:presentation. 2026-06-22 increase is limited to PlazaScreen Compose Resources migration and the previous broken filter copy fix; no new state-machine responsibility was added.
profile|926|Profile UI shell remains in composeApp; state holder, credential operations and tests are now owned by feature:auth:presentation. P8 copy resourceization temporarily increases imports while keeping ProfileScreen and SettingsDialog hardcoded UI copy at zero.
