# format: schemaPath|testPath|scope
# `legacy-debt` 表示迁移期既有 schema 暂无独立 migration 测试；新增 schema 不得使用该标记。
shared/src/commonMain/sqldelight/com/runninghub/shared/db/DiscoveryCache.sq|legacy-debt|legacy discovery cache schema retained in shared until database ownership is migrated
